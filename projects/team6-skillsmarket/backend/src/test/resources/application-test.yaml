spring:
  application:
    name: demo-test
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:dummy-key-for-test}
      embedding:
        options:
          model: text-embedding-3-small
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    defer-datasource-initialization: false
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      mode: never
