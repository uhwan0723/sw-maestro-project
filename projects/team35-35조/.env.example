# Docker Compose host ports
BACKEND_PORT=8000
FRONTEND_PORT=5173
DB_PORT=5432

# PostgreSQL defaults used by docker-compose.yml
POSTGRES_DB=soma17ai35
POSTGRES_USER=soma
POSTGRES_PASSWORD=soma

# Backend connection string for running the API directly on the host
DATABASE_URL=postgresql+psycopg://soma:soma@localhost:5432/soma17ai35

# Frontend -> Backend API endpoint exposed on the host
VITE_API_BASE_URL=http://localhost:8000
