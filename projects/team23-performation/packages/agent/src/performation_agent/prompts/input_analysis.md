# Input Analysis Prompt Draft

This placeholder exists so prompt work can be versioned separately from workflow wiring.

Future prompt goals:

- classify venue name, concert name, and detail question
- extract venue name, artist name, event date, and detail intent
- return structured JSON for `GuideState`

